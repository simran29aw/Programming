#include<iostream>
using namespace std;
int main()
{
int n,a,rev=0,rem;
cin>>n;
a=n;
while(n!=0)
{
rem=n%10;
rev=rem+(rev*10);
n=n/10;
}
if(rev==a)
cout<<"pallindrome";
else
cout<<"not pallindrome";
return 0;
}
